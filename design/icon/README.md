# 已选定的应用图标

`selected-icon.png` 是用户选定的草方块服务器图标，浅灰蓝背景，由内置 imagegen 生成并经用户确认。

首页使用 `app_brand.png`（512 px）；启动图标使用 `app_launcher.png`（432 px），主体缩至画布的安全区域，外沿延展背景，以适配圆形与圆角方形遮罩。

原始生成提示要点：保留草方块服务器的几何、角度、绿色草顶、深灰机身与绿色状态灯，只把深绿背景改成浅灰蓝渐变（约 #E3EDF5 到 #C5D5E4），增加轻柔接触阴影，不加文字或装饰。
